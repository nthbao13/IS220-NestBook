@{
    ViewBag.Title = "Lỗi không thể truy cập";
    Layout = null; /* Hoặc Layout = "~/Views/Shared/_LayoutLogin.cshtml"; nếu bạn có layout riêng cho trang login */
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Truy cập bị từ chối</title>
    <style>
        :root {
          --primary-brown: #8B4513;
          --secondary-brown: #A0522D;
          --light-brown: #D2B48C;
          --bg-cream: #f5f3f0;
        }

        body {
          margin: 0;
          font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
          background-color: var(--bg-cream);
          color: var(--primary-brown);
          display: flex;
          justify-content: center;
          align-items: center;
          height: 100vh;
        }

        .error-card {
          background-color: white;
          border: 1px solid var(--light-brown);
          border-radius: 12px;
          padding: 30px 40px;
          box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
          text-align: center;
          max-width: 400px;
          width: 90%;
        }

        .error-card h1 {
          font-size: 2em;
          color: var(--secondary-brown);
          margin-bottom: 10px;
        }

        .error-card p {
          font-size: 1em;
          margin-bottom: 20px;
        }

        .btn-home {
          background-color: var(--primary-brown);
          color: white;
          padding: 10px 20px;
          border: none;
          border-radius: 8px;
          font-size: 1em;
          cursor: pointer;
          transition: background-color 0.3s ease;
        }

        .btn-home:hover {
          background-color: var(--secondary-brown);
        }
    </style>
</head>
<body>
    <div class="error-card">
        <h1>403 - Truy cập bị từ chối</h1>
        <p>Bạn không có quyền truy cập vào trang này. Vui lòng kiểm tra lại tài khoản hoặc liên hệ với quản trị viên.</p>
        <button class="btn-home" onclick="window.location.href='/'">Về trang chủ</button>
    </div>
</body>
</html>
